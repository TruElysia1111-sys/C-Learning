#include <iostream>
using namespace std;
int main_() {
    // 关键字，C++中预先保留的单词
    // 在定义变量或常量时，不要使用关键字
    /*
        friend      goto       register    union
        void        inline      signed      unsigned
        volatile    case        short       while
        catch        sizeof      typeid       using
        class        static      typename     wchar_t
        const       struct      union       explicit
        const_cast  template    virtual      xor
        delete      this         volatile    xor_eq
        dynamic_cast   true      while       false
        explicit    try          xor_eq      friend
        export      typeid       namespace   operator
        extern      typename     new         private
        false       using        operator    protected
        float       virtual      public      sizeof
        for         volatile     reinterpret_cast
        friend      while        static_cast
        goto        wchar_t      static_cast
        if          class        struct      switch
    */


    std::cout << "Hello, World!" << std::endl;
    return 0;
}